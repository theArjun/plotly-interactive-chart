[project]
name = "plotly-interactive-chart"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.1",
    "plotly[express]>=6.6.0",
    "polars>=1.39.0",
    "pyarrow>=23.0.1",
]
